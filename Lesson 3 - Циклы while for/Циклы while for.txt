#include "iostream"

using namespace std;

int main() {


	// Task 1


	
	int num,sum=0;
	cout << "Vvedite cislo: ";
	cin >> num;

	for (int i = 1;  i <= num; i++)
	{
		sum += i;
	}

	cout << sum;

	

	// Task 2

	
	int num, factorial=1;
	cout << "Vvedite cislo: ";
	cin >> num;

	while (0 < num) {
		factorial *= num;
		num--;
	}
	cout << factorial;
	

	// Task 3

	

	int num,i=2;
	bool flag = true;
	cout << "Vvedite cislo: ";
	cin >> num;

	do{
		if (num == 0 || num == 1) {
			cout << "Ni otnositsa ni kkomu!";
			flag = false;
			break;
		}
		else if (num == i) {
			cout << "Prostoe!";
			flag = false;
			break;
		}
		else if (num%i==0) {
			cout << "Cislo ne prostoe!";
			flag = false;
			break;
		}
		i++;
		
	} while (i < num);

	if (flag) {
		cout << "Prostoe!" << endl;
	}
	

	// Task 4
	

	int num;
	cout << "Vvedite cislo: ";
	cin >> num;

	for (num;  1<= num; num--)
	{
		cout << num <<endl;

	}
	

	// Task 5
	

	int num, sum = 0;
	cout << "Vvedite cislo: ";
	cin >> num;

	while (num >= 1) {
		if (num % 2 == 0) {
			sum += num;
		}
		num--;
	}

	cout << sum;
	

	// Task 6

	int num;
	cout << "Vvedite cislo: ";
	cin >> num;

	for (int i = 1; i <= 10; i++)
	{
		cout << num << " * " << i << " = " << num * i << endl;
	}


	return 0;
}