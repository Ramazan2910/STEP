#include <iostream>
#include <string>

using namespace std;

//Task 1

struct Washing_machine {
	char firm[20];
	char color[20];
	char width[20];
	char height[20];
	int power;
	int speed;
	int temperature;

	void Show() {
		cout << "Firm: " << firm << "\n" << "Color: " << color << "\n" << "Width: " << width << "\n" << "Height: " << height << "\n" << "Power: " << power << "\n" << "Speed: " << speed << "\n" << "Temperature: " << temperature;
	}
};

// Task2

struct Iron {
	char firm[20];
	char model[20];
	char color[20];
	int min_temp;
	int max_temp;
	bool steam;
	int power;


	void Show() {
		cout << "Firm: " << firm << "\n" << "Model: " << model << "\n" << "Color: " << color << "\n" << "Min_temp: " << min_temp << "\n" << "Max_temp: " << max_temp << "\n" << "Steam: " << boolalpha << steam << "\n" << "Power: " << power;
	}
};


// Task 3

struct Boiler {
	char firm[20];
	char color[20];
	int power;
	int capacity;
	int temperature;


	void Show() {
		cout << "Firm: " << firm << "\n" << "Color: " << color << "\n" << "Capacity: " << capacity << "\n" << "Temperature: " << temperature << "\n" << "Power: " << power;
	}
};


// Task 4

struct Animals {
	char name[20];
	char klass[20];
	char clicka[20];


	void Add() {
		cout << "Vvedite name: ";
		cin >> name;
		cout << "Vvedite klass: ";
		cin >> klass;
		cout << "Vvedite clicka: ";
		cin >> clicka;
	}
	void Show() {
		cout << "\n" << "Name: " << name << "\n" << "Class: " << klass << "\n" << "Cliska: " << clicka;
	}

	void Voice() {
		cout << "\nQolos -> Privet !";
	}
};

// Task 5

struct Book {
	string name;
	string autor;
	string izadel;
	string janr;

	void Edit(Book& fbook) {
		bool edit_flag = true;
		while (edit_flag) {
			int choise;
			cout << endl;
			cout << "\t Menu Edit \n1. Name\n2. Autor\n3. Izadel\n4. Janr\n5. Exit\n";
			cout << "Vas vibor: ";
			cin >> choise;
			if (choise == 1) {
				string edit_name;
				cout << "Vvedite zamenu name: ";
				cin >> edit_name;
				fbook.name = edit_name;
			}
			else if (choise == 2) {
				string edit_autor;
				cout << "Vvedite zamenu autor: ";
				cin >> edit_autor;
				fbook.autor = edit_autor;
			}
			else if (choise == 3) {
				string edit_izadel;
				cout << "Vvedite zamenu izadel: ";
				cin >> edit_izadel;
				fbook.izadel = edit_izadel;
			}
			else if (choise == 4) {
				string edit_janr;
				cout << "Vvedite zamenu janr: ";
				cin >> edit_janr;
				fbook.janr = edit_janr;
			}
			else if (choise == 5) {
				cout << endl;
				edit_flag = false;
			}
		}
	}

	void Show_all(Book fbook) {
		cout << "Name: " << fbook.name << "   " << "Autor: " << fbook.autor << "   " << "Izadel: " << fbook.izadel << "   " << "Janr: " << fbook.janr << endl;
	}

	void Autor_search(Book& fbook, string fautor, int findex) {
		if (fbook.autor == fautor) {
			cout << "Vot vasa kniqa: " << endl;
			cout << "Name: " << fbook.name << "   " << "Autor: " << fbook.autor << "   " << "Izadel: " << fbook.izadel << "   " << "Janr: " << fbook.janr << "\n";
			cout << endl;
		}
	}

	void Name_search(Book& fbook, string fname, int findex) {
		if (fbook.name == fname) {
			cout << "Vot vasa kniqa: " << endl;
			cout << "Name: " << fbook.name << "   " << "Autor: " << fbook.autor << "   " << "Izadel: " << fbook.izadel << "   " << "Janr: " << fbook.janr << "\n";
			cout << endl;
		}
	}
};

int main() {

	//Task 1
	
	Washing_machine machine = { "Bosch","White","150","180",50,70,40 };
	machine.Show();
	

	// Task 2

	Iron machine = { "Bosch","ZH-500","White",50,100,true,40 };
	machine.Show();
	

	// Task 3

	Boiler machine = { "Bosch","White",50,40,100 };
	machine.Show();
	
	
	// Task 4
	Animals animal;
	animal.Add();
	animal.Show();
	animal.Voice();
	


	// Task 5
	bool flag = true;
	Book books[5];
	books[0] = { "Voyna i mir", "Lev tolstoy","AzTU","Roman" };
	books[1] = { "Voyna i mir1", "Lev tolstoy1","AzTU1","Roman1" };
	books[2] = { "Voyna i mir2", "Lev tolstoy2","AzTU2","Roman2" };
	books[3] = { "Voyna i mir3", "Lev tolstoy3","AzTU3","Roman3" };
	books[4] = { "Voyna i mir4", "Lev tolstoy4","AzTU4","Roman4" };


	while (flag) {
		int choise;
		cout << "\t Vas vibor \n1. Edit\n2. Show all\n3. Autor search\n4. Name search\n5. Exit\n";
		cout << "Vas vibor: ";
		cin >> choise;
		if (choise == 1) {
			int edit_choise;
			cout << endl;
			cout << "Kakuyu kniqu menayem: ";
			cin >> edit_choise;
			books->Edit(books[edit_choise]);
		}
		else if (choise == 2) {
			cout << endl;
			for (int i = 0; i < 5; i++)
			{
				books->Show_all(books[i]);
			}
			cout << endl;
		}
		else if (choise == 3) {
			int autor_index = 0;
			string autor_search;
			cout << "Kakoy autor: ";
			cin.ignore();
			getline(cin, autor_search);
			cout << endl;
			for (int i = 0; i < 5; i++)
			{
				books->Autor_search(books[i], autor_search, autor_index);
				autor_index++;
			}
		}

		else if (choise == 4) {
			int name_index = 0;
			string name_search;
			cout << "Kakoe nazvaniye kniqi: ";
			cin.ignore();
			getline(cin, name_search);
			cout << endl;
			for (int i = 0; i < 5; i++)
			{
				books->Name_search(books[i], name_search, name_index);
				name_index++;
			}
		}
		else if (choise == 5) {
			flag = false;
		}
	}




	return 0;
}