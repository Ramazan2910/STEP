friends funktions or class

//////////////////////////

friends class


#include <iostream>
#include<string>
using namespace std;

class Human; // Step 4 - nujno takje opredelit class Human( v nashem sluchae realizaciya Human po otnosheniyu Apple vishe to mojno bilo bi eto ne dobavit)
class Apple; // Step 3 - nujno takje opredelit class Apple vishe chem Human( no ne realizaciyu)!

class Human {
public:
    void TakeApple(Apple& apple); // Nado sozdat takoe opredelenie inache vnutri peremennix ne uvidit, a realizaciyu nije opisivaet// Step 1 - chtobi imet dostup privatnim polam  v klasse apple sdelaem ego drujestvennim
        

    void EatApple(Apple &apple){

    }
};

class Apple {
    
public:
    Apple(int weight, string color ) {
        this->weight = weight;
        this->color = color;
    }
private:
    int weight;
    string color;
    friend void Human::TakeApple(Apple& apple);// Step 2 - sdelali drujestvennoy // takje danniy klass(Human) doljen vishe stoyat chem Apple


};


int main() {

    Apple apple(150, "Red");
    Human human;
    human.TakeApple(apple);

    return 0;
}

void Human::TakeApple(Apple& apple) {
    cout << "TakeApple " << "weight = " << apple.weight << " color = " << apple.color << endl;
}

..........................................................................................................


drujestvennie funkcii:


#include <iostream>
#include<string>
using namespace std;

class Person {
private:
    int age;
    string name;

public:
    Person(int age, string name) {
        this->age = age;
        this->name = name;
    }

    friend void Info(Person& person); // doljni ukazat v klasse chto eto drujestvennaya funkciya
};

void Info(Person& person) {
    cout << person.name << person.age << endl;
}

int main() {
    Person person1(27, "Tom");

    Info(person1);
    return 0;
}


....................................................................................................




