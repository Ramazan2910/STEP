#include <iostream>
#include<string>

using namespace std;

struct Item {
	char brand[25];
	char model[25];
	int count;
	int price;
};

struct Storekeeper {
	Item items[10];

	void Add(int counter) {
		cout << "Vvedite brand: ";
		cin >> items[counter].brand;
		cout << "Vvedite model: ";
		cin >> items[counter].model;
		cout << "Vvedite count: ";
		cin >> items[counter].count;
		cout << "Vvedite price: ";
		cin >> items[counter].price;
	}

	void Delete(int counter) {
		int del;
		cout << "Vvedite index cto udalayem: ";
		cin >> del;

		for (int i = 0; i < counter + 1; i++)
		{
			if (i == del) {
				int counter2 = i;
				while (counter2 < counter) {
					items[counter2] = items[counter2 + 1];
					counter2++;
				}
			}
		}
	}

	void Edit(int counter) {
		char object[25];
		char change[25];
		cout << "Vvedite model: ";
		cin >> object;
		cout << "Vvedite na cto: ";
		cin >> change;
		for (int i = 0; i < counter; i++)
		{
			if ((strcmp(items[i].model, object) == 0)) {
				for (int j = 0; j < size(change); j++)
				{
					items[i].model[j] = change[j];
				}
			}
		}
	}

	void Show(int counter) {
		for (int i = 0; i < counter; i++)
		{
			cout << items[i].brand << " " << items[i].model << " " << items[i].count << " " << items[i].price << endl;

		}
	}

};

struct Adress {
	char country[15];
};

struct User {
	char login[20];
	char password[20];
	Adress adress;
};

int main() {
	User User1 = { "admin1","admin124","Azerbaycan" };
	User User2 = { "admin2","admin123","Azerbaycan2" };

	bool flag1 = true;
	bool flag2 = true;

	Storekeeper helo;
	int counter = 0;

	while (flag1)
	{
		char login[20];
		char password[20];
		cout << "Vvedite login: ";
		cin >> login;
		cout << "Vvedite login: ";
		cin >> password;
		if (((strcmp(User1.login, login) == 0) && (strcmp(User1.password, password) == 0)) || ((strcmp(User2.login, login) == 0) && (strcmp(User2.password, password) == 0))) {
			while (flag2)
			{
				cout << "\tVas vibor\n1.Add\n2.Delete\n3.Edit\n4.Show\n5.Exit\n";
				int choise;
				cout << "Vvedite cislo: ";
				cin >> choise;
				switch (choise)
				{
				case 1:
					helo.Add(counter);
					counter++;
					break;
				case 2:
					helo.Delete(counter);
					counter--;
					break;
				case 3:
					helo.Edit(counter);
					break;
				case 4:
					helo.Show(counter);
					break;
				case 5:
					flag2 = false;
					break;
				default:
					break;
				}
			}
			flag1 = false;
		}
		else {
			cout << "Nepravelniy login ili parol!"<<endl;
		}
	}


	return 0;


}